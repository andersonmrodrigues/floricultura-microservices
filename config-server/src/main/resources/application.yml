server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andersonmrodrigues/floricultura-microservices/tree/main/repo
          search-paths: fornecedor
